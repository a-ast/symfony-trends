default:
    suites:
        api_features:
            paths:    [ %paths.base%/features ]
            contexts:
                - ApiFeatureContext:
                      dispatcher: '@event_dispatcher'
                      client:     '@github_api.client'


    extensions:
        Behat\Symfony2Extension: ~
