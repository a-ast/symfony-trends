default:
    suites:
        api_features:
            paths:    [ %paths.base%/features ]
            contexts:
                - ApiFeatureContext:
                    client: '@github_api.github_api_client'


    extensions:
        Behat\Symfony2Extension: ~
